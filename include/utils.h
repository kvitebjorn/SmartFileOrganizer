#ifndef UTILS_H
#define UTILS_H
#include <stdint.h>
#include <stdlib.h>
#include <stdbool.h>

bool      cmp_buffers (const uint8_t  *restrict buff1, const uint8_t *restrict buff2, size_t lenght);
#endif
